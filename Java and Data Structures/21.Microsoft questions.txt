Microsoft sums :-

1. tree zigzag level order traversal

use q of nodes, change flag after every level, if straight order add to the end of linked list (ll.add () ) else addFirst !

things to remember 
1. list of lists 
List<List<Integer> returnList = new ArrayList<>() ;

2. Defining queue
Queue<TreeNode> q = new LinkedList<>();
-------------------------------------------